# dist/ is generated: the ncc bundle plus the vendored scan CLI, third-party
# code we do not maintain. Dependency vulnerabilities in it are covered by the
# dependency graph, not by this analysis.
sonar.exclusions=dist/**
